# Runtime dependencies for the llms.2plot.dev documentation site (app.py).
#
# dash-improve-my-llms is deliberately NOT listed here. The Dockerfile installs
# it from this working tree with `pip install --no-deps .`, because this host
# exists to demonstrate the current code — pinning the published release would
# make the reference deployment lag the thing it documents. For the package's
# own dependency set see pyproject.toml.

dash>=4.1,<5
flask>=2.0.0
# >=23: 21.x/22.x are stuck on CVE-2024-6827 / CVE-2024-1135 (request
# smuggling). Network floor for every host gunicorn fronts.
gunicorn>=23

# Demo app UI and the /analytics page.
dash-mantine-components>=2.3.0
numpy>=1.24

# The satellite analytics trio (lib/analytics_tracker + satellite_reporter):
# the geolocation fallback and the signed rollup POST to 2plot.ai. dash pulls
# requests transitively, but the trio depends on it directly — a transitive
# dep is one dash release away from vanishing.
requests>=2.31
